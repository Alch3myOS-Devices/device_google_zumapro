get_prop(audioserver, vendor_audio_prop)

set_prop(audioserver, vendor_audio_prop)

allow audioserver audio_device:chr_file r_file_perms;

dontaudit audioserver hal_power_stats_service:service_manager find;